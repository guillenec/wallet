export const GenericTitle = ({ title }) => {
  return (
    <section className="genericTitle w-full flex justify-center items-center text-center mt-10">
      <h2 className="font-titulo font-bold text-secundario text-2xl">
        {title}
      </h2>
    </section>
  )
}
